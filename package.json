{
  "name": "planning-iut-calais",
  "version": "2.1.0",
  "private": true,
  "description": "A microservice that periodically screenshots Calais's IUT planning using Puppeteer and serves it 📅",
  "main": "index.js",
  "scripts": {
    "start": "node index.js --server --service",
    "start:server": "node index.js --server",
    "start:service": "node index.js --service",
    "start:service:show": "node index.js --service --no-headless",
    "clean": "rm -rf db.json screenshots",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "rigwild <contact@asauvage.fr>",
  "license": "MIT",
  "dependencies": {
    "cron": "^1.7.2",
    "express": "^4.17.1",
    "fs-extra": "^8.1.0",
    "lowdb": "^1.0.0",
    "node-fetch": "^2.6.0",
    "puppeteer": "^1.19.0"
  },
  "devDependencies": {
    "eslint": "^6.3.0"
  }
}
